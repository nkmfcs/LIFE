version: '3.8'

services:
  life-web:
    build: .
    container_name: life-web
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env.production
    network_mode: host
